editedAnswer = document.getElementById('answer-<%= @answer.id %>')
editedAnswer.querySelector('.inline-answer-errors').innerHTML = '<%= render 'shared/errors', resource: @answer %>'

<% unless @answer.errors.present? %>
  editedAnswer.innerHTML = '<%= j render @answer, answer_class: nil %>'
<% end %>
