.answer  id = "#{answer.id}"
  p = answer.body

  -if current_user&.author_of?(answer)
    p.btn = link_to 'Delete answer', answer_path(answer), remote: true, method:  :delete unless answer.new_record?
