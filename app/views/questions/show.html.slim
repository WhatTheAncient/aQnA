h1 = @question.title
h4 = @question.body
-if current_user&.author_of? (@question)
  p.btn = link_to 'Delete question', question_path(@question),
          method:  :delete

= render @question.answers

= render 'answers/form'
